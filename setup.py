from setuptools import setup, find_packages

setup(
    name='usa_visa_predictor',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies that should be installed with your package
    ],
)
